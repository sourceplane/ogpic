apiVersion: sourceplane.io/v1
kind: Component
metadata:
  name: notifications-client-tests
  title: Notifications Client Tests
  description: Test suite for the notifications-client package.
spec:
  type: turbo-package
  lifecycle: production
  system: quality
  owner: platform
  docs:
    overview: overview.md
    pages:
      - { path: ../../docs/testing/triage-runbook.md, role: runbook }
  domain: platform
  language: typescript
  tags:
    - test
    - typescript
  links:
    - title: Source
      url: https://github.com/sourceplane/ogpic/tree/main/tests/notifications-client
      icon: github
  subscribe:
    environments:
      - name: dev
        profile: quick-check
  parameters:
    nodeVersion: "22"
    pnpmVersion: "10.12.1"
    buildCommand: pnpm exec turbo run build --filter=./
    typecheckCommand: pnpm exec turbo run typecheck --filter=./
    preBuildCommand: pnpm exec turbo run test --filter=./
  labels:
    team: platform
    layer: test
# ci: trigger full-component verification (orun v2.12.0 upgrade — PR #183)
