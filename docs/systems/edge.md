# Edge system

The single public HTTP entry point: TLS, tenancy resolution, rate-limit
families, contract-version enforcement, and per-context facades over service
bindings. One route surface to defend; every worker behind it stays private.
See the [edge architecture](../../apps/api-edge/architecture.md).
