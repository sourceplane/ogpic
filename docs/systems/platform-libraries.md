# Platform libraries system

The shared workspace packages every service compiles against: contracts,
db (migrations + repositories), sdk, cli, policy-engine, shared utilities,
testing harness, notifications-client, webhook-verifier. Versioned by the
workspace — merging is releasing.
