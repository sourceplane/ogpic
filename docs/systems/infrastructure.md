# Infrastructure system

The ground the platform stands on: Supabase Postgres, Cloudflare Hyperdrive/
KV/domains via Terraform, the migration lane, and the stack-tectonic golden
paths every component builds and deploys through.
