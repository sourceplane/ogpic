# Storefront

The customer-facing product experience for the gear-rental marketplace — the
web console rendering every surface over the public API. A deliberately thin
client: if the console can do it, the API can, with identical authorization.

Components: `web-console-next`.
